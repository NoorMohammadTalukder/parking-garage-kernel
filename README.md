# Parking Garage Kernel Module

A Linux kernel module that implements a parking garage
management system.

## Features
- /proc/parking shows live garage status
- Multiple car types (Compact, SUV, Truck)
- Per floor car tracking
- Thread safe with mutex locks
